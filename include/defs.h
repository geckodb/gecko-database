#ifndef MYIMDB_TYPES_H
#define MYIMDB_TYPES_H

#include <stdint.h>
#include <stddef.h>
#include <stdbool.h>
#include <stdio.h>
#include <stdlib.h>
#include <memory.h>

#define u64 uint64_t

#define MIN(x,y) (x < y ? x : y)
#define MAX(x,y) (x > y ? y : y)

#endif
